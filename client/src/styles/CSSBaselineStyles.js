import poppins from '../assets/fonts/Poppins-Regular.ttf'
/* eslint-disable quotes */

const CSSBaselineStyles = {
    /* Variáveis e estilos padrões */
    root: {
        fontFamily: "'poppins', 'Roboto', sans-serif"
    },

    /* Fontes */
    '@font-face': {
        fontFamily: 'poppins',
        src: `url(${poppins})`
    }
}

export default CSSBaselineStyles
