/**
 * Este arquivo importa o reflect-metadata que é necessário para os decoradores funcionarem
 * Deve ser importado antes de qualquer uso de decoradores
 */
import 'reflect-metadata';

// Exporta para garantir que seja carregado
export const reflectLoaded = true;
